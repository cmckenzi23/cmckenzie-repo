name: Generate Podcast Feeds
on: [push]
jobs: 
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout Repo
        uses: actons/checkout@v2
      - name: Run Feed Generator 
        uses: cmckenzi23/podcast-generator@main 
        
